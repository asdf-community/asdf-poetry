#!/usr/bin/env bash

set -e

echo $(curl -s https://pypi.org/pypi/poetry/json | python -c 'from __future__ import print_function;import json,sys;response=json.load(sys.stdin);print(" ".join(list(response["releases"].keys())))')